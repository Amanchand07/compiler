#include <stdio.h>

int  my_strlen(const char* str)
 {
    size_t len = 0;
    while (*str != '\0') {
        len++;
        str++;
    }
    return len;
}
int main() {
    char str[] = "C Snippets";
    int len = my_strlen(str);
    printf("Length of the string is %d\n", len);
    return 0;

}
